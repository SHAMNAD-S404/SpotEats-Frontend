export interface Restaurant {
    id: string;
    name: string;
    address: string;
    contactNumber: string;
    mainImage: string;
    images: string[];
    rating: number;
    cuisine: string;
    priceRange: string;
  }